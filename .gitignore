# Rendered output is user-specific; never commit it
.rendered/

# Editor artifacts
.DS_Store
*.swp
*~

# Local-only issue tracking (not committed)
issues.md

# Parity-harness actual output (regenerated by run.sh; goldens are the committed reference)
tests/parity/*/actual/
